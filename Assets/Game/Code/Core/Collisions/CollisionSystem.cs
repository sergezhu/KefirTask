namespace Game.Code.Core.Collisions
{
	/*using System.Collections.Generic;
	using Game.Code.Unity.Common;
	using UnityEngine;

	public class CollisionSystem : ITickable
	{
		private Dictionary<IPresenter, ICollidedView> _collidersDictionary;

		public CollisionSystem()
		{
			_collidersDictionary = new Dictionary<IPresenter, ICollidedView>();
		}

		public void AddPresenter( IPresenter presenter, ICollidedView view )
		{
			_collidersDictionary.Add( presenter, view );
		}

		public void RemovePresenter( IPresenter presenter )
		{
			_collidersDictionary.Remove( presenter );
		}

		public void Tick( float deltaTime )
		{
			foreach ( var VARIABLE in COLLECTION )
			{
				
			}
		}
	}*/
}